# บันทึกการทำงาน — 7 ก.ย. 2026

โจทย์: ยก Tripster จากแอปจัดทริปส่วนตัว (ภูเก็ต, localStorage, ไฟล์เดียว)
ไปเป็นเวอร์ชันที่คนทั่วไปใช้ได้ โดยเริ่มจากคิด flow → ทำ wireframe → สรุปลง UI

branch: `claude/awesome-pasteur-7mt2sr`

---

## สิ่งที่ตัดสินใจไว้ (เป็นฐานของงานทั้งหมด)

| เรื่อง | ที่เลือก |
|---|---|
| การเก็บข้อมูลผู้ใช้ | Guest ใช้ได้ทันที แล้วค่อยสมัครเมื่อจะแชร์/ซิงก์ |
| ขอบเขตจุดหมาย | ทั่วไทย 77 จังหวัด |
| ฟีเจอร์สังคม | ครบทุกระดับ — ลิงก์อ่านอย่างเดียว, ร่วมแก้, ฟีดชุมชน |
| ภาษา | ไทยเป็นหลัก |

---

## ไฟล์ที่เพิ่มวันนี้

ทั้งสองไฟล์เป็นไฟล์ใหม่แยกต่างหาก **`index.html` เดิมไม่ถูกแตะเลย**

### 1. `design-v2.html` — พิมพ์เขียวเวอร์ชันสาธารณะ
commit `908e0d1` · https://claude.ai/code/artifact/f1d1b13b-bf90-4dba-a934-1abcaf127558

- **ส่วน 00** ตารางเทียบ v1.01 กับ v2 ทั้ง 9 เรื่อง + 3 เรื่องที่ต้องเคาะก่อนเขียนโค้ด
- **ส่วน 01** หลักคิด 5 ข้อ, โครงข้อมูลใหม่, โครงหน้าจอ 5 แท็บ, flow หลัก 6 เส้น
- **ส่วน 02** wireframe 42 หน้าจอ แบ่ง 9 กลุ่ม (A–I) แต่ละหน้ามีคำอธิบายรายข้อ
  ที่ตรงกับตัวเลขบนภาพ และบอกทางเข้า–ทางออกของหน้านั้น
- **ส่วน 03** สรุป UI — สี ฟอนต์ คอมโพเนนต์ 12 ตัว การวางบนจอต่าง ๆ การเข้าถึง
  จุดที่ต้องแก้ในโค้ดจริง และลำดับการทำ 3 เฟส

### 2. `wireframe-flows.html` — wireframe low-fi เลือกดูตาม flow
commit `5410d51` (แก้ layout มือถือใน commit ถัดมา) ·
https://claude.ai/code/artifact/ee6311bb-be86-4bee-a1df-0b8e898e8ba3

หน้าเดียว มีเมนู flow ด้านซ้าย กดแล้วฝั่งขวาเปลี่ยนทั้งชุด · เลือก flow จาก
URL hash ได้ (`#signup`, `#login`) จึงส่งลิงก์ตรงไปที่ flow เดียวได้

| # | Flow | เส้นทางหลัก |
|---|---|---|
| 01 | เปิดแอปครั้งแรก | A1 → A2 → A3 → A4 → A5 → A6 → D3 |
| 02 | สมัครสมาชิก | G1 → G2 → G2b → G3 |
| 03 | เข้าสู่ระบบ | G4g → G2 → G2b → D1 |
| 04 | ค้นหาที่เที่ยว → เพิ่มเข้าแผน | C1 → C3 → C4 → C5 → D4 → D3 |
| 05 | จัดแผนทริป | D1 → D3 → D4 → D5 → D6 |
| 06 | แชร์ทริป | D3 → E1 → G1 → E2 |
| 07 | ร่วมแก้กับเพื่อน | E1 → E3 → H1 → E4 → D5 |
| 08 | ก๊อปทริปคนอื่น | F1 → E2 → E5 → D3 |
| 09 | วันเดินทางจริง | B3 → H2 → I1 → H3 → C5 |

ทุก flow มีแถว "ทางแยก" แยกไว้ใต้เส้นหลัก · หน้าจอ 38 หน้าเก็บเป็นข้อมูลใน
สคริปต์แล้วประกอบเป็นภาพตอนโหลด จึงใช้ซ้ำข้าม flow ได้โดยไม่ต้องวาดซ้ำ

**บั๊กที่เจอและแก้แล้ว** — บนมือถือ เมนูด้านซ้ายดันความกว้างของ grid column
จนทั้งหน้าเลื่อนออกด้านข้าง (scrollWidth 1341 บนจอ 485) แก้ด้วยการใส่
`min-width:0` ให้ `.side` และ `.nav` แล้วให้การ์ด wireframe เรียงเดี่ยว
กว้างเต็มคอลัมน์เมื่อจอแคบกว่า 560px

---

## ข้อสรุปจากการอ่านโค้ดเดิม (ใช้อ้างอิงต่อได้)

- โครงปัจจุบัน: 4 แท็บ (หน้าแรก / แผนทริป / ค้นหา / แจ้งเตือน), 66 สถานที่,
  พิกัดครบทุกที่, รูป 24 ใบฝังเป็น data URI, ไฟล์รวม 3.3 MB
- กลไกที่ทำไว้ดีแล้วและควรเก็บ: `openPickSheet` + `pickRef` (เลือกทีละหลายที่
  พร้อมบอกระยะจากจุดล่าสุด), `parseMapsUrl` (แกะลิงก์ Google Maps 4 แบบ),
  `routeUrl` (นำทางทั้งวัน), `legKm` (Haversine), กลไกเตือนเข้าใกล้ใน v1.02
- ข้อจำกัดที่หายไปเมื่อย้ายออกจาก Artifact: CSP ที่เคยบล็อก Maps API,
  iframe และ fetch ข้ามโดเมน — ทำให้ทำแผนที่จริงและตามลิงก์ย่อ
  `maps.app.goo.gl` ได้

---

## ที่ยังต้องตัดสินใจก่อนเริ่มเขียนโค้ดเฟส 1

1. **ลิขสิทธิ์รูป 24 ใบที่ฝังอยู่** — README ของโปรเจกต์เองระบุว่ารูปจากเว็บรีวิว
   ใช้ส่วนตัวได้ แต่ทำโปรดักต์สาธารณะไม่ได้ ต้องคัดใหม่ก่อนเปิด
2. **แหล่งข้อมูลสถานที่ทั่วไทย** — จากผลทดสอบ 58 ที่ในภูเก็ต ร้านเล็ก 43 ที่
   ไม่มีทั้งใน Wikidata และ OSM เลยสักร้าน ถ้าจะครอบคลุมทั่วไทยต้องพึ่ง
   Places API (มีค่าใช้จ่ายต่อ request) หรือให้ชุมชนช่วยเติมแล้วมีคนตรวจ
3. **เงื่อนไขการใช้งานและคิวตรวจเนื้อหา** — ต้องมีก่อนเปิดฟีดสาธารณะ (เฟส 3)

## ขั้นถัดไปที่คุยกันไว้

ทำ UI จริงของเฟส 1 (A1–A6 + กลุ่ม B) เป็น HTML ต่อจาก wireframe
